import Container from 'react-bootstrap/Container';

function ITCActor(props) {

  return (
    <Container className={props.className}>
      <h1 className="text-white">Actor</h1>
    </Container>
  );
}

export default ITCActor;
